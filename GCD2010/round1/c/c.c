#include <stdio.h>

int g[i][j];
int main()
{
	int ncase, icase;
	scanf("%d", &ncase);

	for(icase = 1;icase <= ncase;icase++)
	{
		scanf("%d %d", &m, &n);

		for(i = 0;i < m;i++)
		{

		}
	}
