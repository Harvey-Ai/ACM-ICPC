#include <stdio.h>

int main()
{
	int flag;
	int N, c1, g1, c2, g2;
	int gg, d1, d2;
	int ncase;
	int flag1, flag2;
	freopen("d.in", "r", stdin);
	freopen("d.out", "w", stdout);
	scanf("%d", &ncase);

	while(ncase--)
	{
		scanf("%d %d %d %d %d", &N, &c1, &g1, &c2, &g2);

	}
	return 0;
}
