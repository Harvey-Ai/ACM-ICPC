#include <stdio.h>
#include <math.h>

int main()
{
	int n, p1, p2, p, dis1, dis2;
	scanf("%d", &n);
	while(n--)
	{
		scanf("%d %d %d", &p, &p1, &p2);
		dis1 = fabs(p - p1);
		dis2 = fabs(p - p2);

		dis1 < dis2 ? printf("A\n") : printf("B\n");
	}
	return 0;
}
