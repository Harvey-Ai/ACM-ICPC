#include <iostream>
using namespace std;

int work(int a)
{
	if (a == 1)
		return 0;
	if (a % 2)
		return work((a * 3 + 1) / 2) + 1;
	else
		return work(a / 2) + 1;
}
int main()
{
	int n;
	while(cin >> n, n)
	{
		int res = work(n);
		cout << res << endl;
	}
	return 0;
}
