#include <iostream>
#include <algorithm>
using namespace std;

bool cmp(const int &a, const int &b)
{
	return a > b ? 1 : 0;
}

int main()
{
	int n, m;
	int arr[100005];
	int i;
	while(cin >> n >> m, n || m)
	{
		for(i = 0;i < n;i++)
			cin >> arr[i];
		sort(&arr[0], &arr[n], cmp);
		if (m > n)
			m = n;
		int flag = 0;
		for(i = 0;i < m;i++)
		{
			if (flag)
				cout << " " << arr[i];
			else
			{
				cout << arr[i];
				flag = 1;
			}
		}
		cout << endl;
	}
	return 0;
}
