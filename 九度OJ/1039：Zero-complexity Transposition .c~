#include <iostream>
using namespace std;

int main()
{
	int n;
	long long arr[100005];
	int i;

	while(cin >> n)
	{
		for(i = 0;i < n;i++)
			cin >> arr[i];

		cout << arr[n - 1];
		for(i = n - 2;i >= 0;i--)
			cout << " " << arr[i];
		cout << endl;
	}
	return 0;
}
