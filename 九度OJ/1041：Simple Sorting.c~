#include <iostream>
#include <algorithm>
using namespace std;

int main()
{
	int i, n;
	int arr[100005];

	while(cin >> n)
	{
		for(i = 0;i < n;i++)
			cin >> arr[i];
		sort(&arr[0], &arr[n]);

		cout << arr[0];

		for(i = 1;i < n;i++)
			if (arr[i] != arr[i - 1])
				cout << " " << arr[i];
		cout << endl;
	}
	return 0;
}
