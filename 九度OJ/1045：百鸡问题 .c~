#include <iostream>
using namespace std;

int main()
{
	int n, i, j;
	while(cin >> n)
	{
		for(i = 0;i <= 100;i++)
			for(j = 0;j <= 100;j++)
				if (5 * i + 3 * j + (100 - i - j + 2) / 3 <= n)
					cout << "x=" << i << ",y=" << j << ",z=" << 100 - i - j << endl;
	}
	return 0;
}
