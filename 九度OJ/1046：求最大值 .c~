#include <iostream>
using namespace std;

int main()
{
	int arr[100];
	int i, max;
	while(cin >> arr[0])
	{
		for(i = 1;i < 10;i++)
			cin >> arr[i];

		max = arr[0];
		for(i = 0;i < 10;i++)
			if (max < arr[i])
				max = arr[i];

		cout << "max=" << max << endl;
	}
	return 0;
}
