#include <iostream>
#include <map>
using namespace std;


map<int, int> luck;
int main()
{
	int n, m;
	int i, a;
	while(cin >> m >> n)
	{
		luck.clear();
		for(i = 0;i < n;i++)
		{
			cin >> a;
			if (luck.count(a) == 0)
				luck[a] = 1;
			else
				luck[a]++;
		}
		map<int, int>::iterator it;
		for(it = luck.begin();it != luck.end();it++)
			if (it->second != m)
			{
				cout << it->first << endl;
				break;
			}
	}
	return 0;
}

