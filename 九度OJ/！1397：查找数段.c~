#include <iostream>
using namespace std;

char templ[100005], str[100005];
int main()
{
	while(scanf("%s", templ) != EOF)
	{
		scanf("%s", str);
		if (strlen(templ) < strlen(str))
			cout << 0 << endl;

		stateNum = 0;
		for(i = 0;templ[i] != '\0';i++)
		{
			

		}
		
	return 0;
}
