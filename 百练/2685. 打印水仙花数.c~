#include <iostream>
using namespace std;

int main()
{
	int i;
	int a, b, c;
	for(i = 100;i < 1000;i++)
	{
		int a = i % 10;
		int b = (i / 10) % 10;
		int c = (i / 100) % 10;

		if (a * a * a + b * b * b + c * c * c == i)
			cout << i << endl;
	}
	return 0;
}
