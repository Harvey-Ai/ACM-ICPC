#include <iostream>
using namespace std;

int main()
{
	int n;
	int i;
	cin >> n;

	for(i = 10;i <= n;i++)
	{
		int sum = 0;
		int temp = i;
		while(temp)
		{
			sum += temp % 10;
			temp /= 10;
		}
		if (i % sum == 0)
			cout << i << endl;
	}
	return 0;
}
