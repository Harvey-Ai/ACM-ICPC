#include <iostream>
using namespace std;

int main()
{
	double h, r;
	cin >> h >> r;
	double area = h * 3.14159 * r * r;
	double barrel = 20000.0 / area;
	int temp = barrel;
	if (temp * 1.0 < barrel)
		cout << temp + 1 << endl;
	else
		cout << temp;
	return 0;
}
