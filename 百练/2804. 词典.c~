#include <iostream>
#include <map>
#include <cstdio>
using namespace std;

int main()
{
	map<string, string> dic;
	string a, b;
	string word;

	while(cin.peek() != '\n')
	{
		cin >> a >> b;
		dic[b] = a;
		getchar();
	}

	while(cin >> word)
	{
	//	cout << word << endl;
	//	cout << dic[word] << endl;
		if (dic.count(word) != 0)
			cout << dic[word] << endl;
		else
			cout << "eh" << endl;
	}		
	return 0;
}
