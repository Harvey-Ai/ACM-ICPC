#include <stdio.h>

int main()
{
	int i;
	int dp[31];
	int n;
	dp[1] = dp[0] = 1;
	while(scanf("%d", &n) != EOF)
	{
		for(i = 2;i <= n;i++)
			dp[i] = dp[i - 1] + dp[i - 2];

		printf("%d\n", dp[n]);
	}
	return 0;
}
